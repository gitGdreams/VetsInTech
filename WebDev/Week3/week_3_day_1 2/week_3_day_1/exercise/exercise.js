var boxElement = document.getElementById("practiceBox")

// how do we executable code?

// create a function here

  // inside the function
  // write the code that will make the practiceBox bigger


  function makeBigger() {
    var boxElement = document.getElementById("practiceBox")
    boxElement.style.height = "200px";
    boxElement.style.width = "200px";
}

// create a function here

  // inside the function
  // write the code that will make the practiceBox "normal"
  function makeNormal() {
    var boxElement = document.getElementById("practiceBox")
    boxElement.style.height = "100px";
    boxElement.style.width = "100px";
  }

// create a function here

  // inside the function
  // write the code that will make the practiceBox smaller
  function makeSmaller() {
    var boxElement = document.getElementById("practiceBox")
    boxElement.style.height = "50px";
    boxElement.style.width = "50px";
  }

